# HeadBlade Germany — Preview Verification

Verification evidence must correspond to the exact commit that is deployed for review. Historical green runs are not sufficient if the branch head changes.

The final evidence set includes CI, Security, Lighthouse, Browser Smoke, pull-request dependency review, preview safety validation, and a public HTTP check of the deployed review URL.
